import 'package:flutter/material.dart';

import '../../design_system.dart';

class GrumpCatImage extends StatelessWidget {
  @override
    Widget build(BuildContext context) {
      return ImageWrapper(Assets.grumpyCat);
    }
}
